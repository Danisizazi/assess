CustomersApp:
	Assumptions:
		-The user has access to MSSQL server (localhost), with CRUD operations permissions.
		-The user can create local database called AssessmentDb.
		-The user can run a sql script and create the schema and the data. The scripts is provided (.\sql\Customers.sql).
		-The user can use Swagger (https://swagger.io/docs/) to test API's.
		-Unit tests are not required for the assessment. Unit tests are covered in the multiple threads assessment. Integration tests will be done using Swagger.
		-When running the application, the user can access http://localhost:5121/swagger/index.html
		-The assessment does not require email and phone number 
		
			